{
  "name": "tap-in-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel backend app",
    "backend": "cd backend && bun run dev",
    "backend:tunnel": "cd backend && bun run dev:tunnel",
    "app": "cd frontend && bun run dev",
    "dev:tunnel": "echo '🚀 Starting backend with ngrok...' && npm run backend:tunnel && echo '\n📱 Now run: npm run app (in a new terminal)'",
    "build": "cd backend && bun install && bun build.js",
    "start": "cd backend && node dist/index.js",
    "lint": "cd backend && bun run lint && cd ../frontend && bun run lint",
    "format": "cd backend && bun run format && cd ../frontend && bun run format",
    "render-build": "cd backend && bun install && bun build.js",
    "render-start": "cd backend && node dist/index.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
